"""Service layer: external API integrations (products/prices, weather, routing)
and the recommendation engine. Modules are added as each feature is built;
keep business rules here rather than in route functions."""

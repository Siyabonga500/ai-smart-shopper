# Everything needed to run the tests and the linters (CI installs this file).
-r requirements.txt

pytest==9.1.1
pytest-flask==1.3.0            # app / client fixtures and the live-server helpers
factory-boy==3.3.3             # model factories in tests/factories.py
responses==0.26.3              # mocks Nominatim / Apify / Parse.bot / Open-Meteo / OSRM in tests
ruff==0.16.8
black==26.5.1
